<?xml version="1.0" encoding="UTF-8"?>
<base:BasePage xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:base="clr-namespace:Shofar;assembly=Shofar"
    xmlns:local="clr-namespace:Shofar.CustomRenderers;assembly=Shofar"
    x:Class="Shofar.Driver.DriverMyRidesPage"
    Title="My Rides">
    <base:BasePage.Content>
        <StackLayout BackgroundColor="White" Orientation="Vertical" HorizontalOptions="FillAndExpand" 
            VerticalOptions="FillAndExpand">
            <ListView ItemSelected="Handle_ItemSelected" CachingStrategy="RecycleElement" 
                x:Name="lstBookings" HasUnevenRows="true" Margin="5" SeparatorVisibility="None" >
                <ListView.ItemTemplate >
                    <DataTemplate >
                        <ViewCell  >
                            <StackLayout Orientation="Horizontal" BackgroundColor="#F4F6F7" 
                                VerticalOptions="FillAndExpand" Padding="10"  >
                                <StackLayout Orientation="Vertical" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" >
                                <Label Text="{Binding DisplayRideId}" FontAttributes="Bold" TextColor="#1263B4" />
                                <Label Text="{Binding DisplayRideName}" FontAttributes="Bold"  />
                                <Label Text="{Binding DisplayRiderContactNumber}" FontAttributes="Bold"  />
                                <Label Text="{Binding DisplayPickupPoint}" FontAttributes="Bold"  />
                                <Label Text="{Binding DisplayDropPoint}" FontAttributes="Bold"  />
                                <Label Text="{Binding DisplayFare}" FontAttributes="Bold"  />
                                <Label Text="{Binding DisplayDistance}" FontAttributes="Bold"  />
                                <Label Text="{Binding DisplayDate}" FontAttributes="Bold"  />
                                <Label Text="{Binding DisplayTime}" FontAttributes="Bold"  />

                                </StackLayout>


                            

                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </base:BasePage.Content>
</base:BasePage>
